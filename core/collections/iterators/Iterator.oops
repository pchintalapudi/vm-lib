package core.collections.iterators;

interface Iterator<E> {

    public get value() -> E;

    public operator++() -> Iterator<E>;
    public operator++(long _) -> Iterator<E>;
    public operator==(Iterator<E> other) -> boolean;
    public operator!=(Iterator<E> other) -> boolean {
        return !(this == other);
    }
}