package core.collections;
import core.collections.Stack;
import core.collections.Queue;

interface Deque<E> implements Stack<E>, Queue<E> {
    public def pushFront(element:E) -> Deque<E>;
    public def pushBack(element:E) -> Deque<E>;
    public def clear() -> Deque<E>;
}